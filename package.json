{
  "name": "canvas-design-editor",
  "version": "1.0.0",
  "description": "A canvas-based design editor with real-time collaboration",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd client && npm start",
    "build": "cd client && npm run build",
    "test": "npm run test:server && npm run test:client",
    "test:server": "cd server && npm test",
    "test:client": "cd client && npm test",
    "test:e2e": "cd e2e && npx playwright test",
    "test:e2e:ui": "cd e2e && npx playwright test --ui",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install && cd ../e2e && npm install"
  },
  "keywords": ["canvas", "design", "editor", "collaboration"],
  "author": "Canvas Design Editor",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
